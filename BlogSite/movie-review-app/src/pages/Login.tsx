import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom'

// need to create context to handle overarching login

// need to create login object to hold values with states

// need to handle submit based on admin or not

// need to creat form with inputs for username, password, and email
// add submit button 
// add dropdown for admin login